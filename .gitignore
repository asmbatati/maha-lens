# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# build / deps (none used, but guard anyway)
node_modules/
dist/
.vite/

# Higgsfield staging — large text-prompt-only originals; the shipped, optimized
# derivatives live in video/ and img/fx/. Kept locally, not committed.
img/higgsfield/
